# GENERATED by `cargo xtask gen-crds` — DO NOT EDIT
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: backupschedules.kopiur.dev
spec:
  group: kopiur.dev
  names:
    categories:
    - kopiur
    kind: BackupSchedule
    plural: backupschedules
    shortNames:
    - kopiasched
    singular: backupschedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.configRef.name
      name: Config
      type: string
    - jsonPath: .spec.schedule.cron
      name: Schedule
      type: string
    - jsonPath: .spec.schedule.suspend
      name: Suspended
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for BackupScheduleSpec via `CustomResource`
        properties:
          spec:
            description: |-
              Cron + `configRef`. One source of `Backup` CRs; pausing it doesn't affect
              in-flight or completed runs. ADR §3.5.
            properties:
              configRef:
                description: |-
                  Reference to a `BackupConfig` CR (used by `Backup.spec.configRef` and
                  `BackupSchedule.spec.configRef`). May cross namespaces, subject to RBAC. ADR §3.4/§3.5.
                properties:
                  name:
                    type: string
                  namespace:
                    nullable: true
                    type: string
                required:
                - name
                type: object
              failedJobsHistoryLimit:
                description: |-
                  Bounds *failed* `Backup` CRs from this schedule. Successful retention is
                  GFS-driven on `BackupConfig.spec.retention` — there is deliberately NO
                  `successfulJobsHistoryLimit` (ADR-0003 §4.4, ADR-0001 §4.4).
                format: uint32
                minimum: 0.0
                nullable: true
                type: integer
              schedule:
                description: Cron schedule with deterministic jitter, timezone, and concurrency controls. ADR §3.5/§4.1.
                properties:
                  concurrencyPolicy:
                    default: Forbid
                    description: What to do when a previous run is still in flight. Closed enum, default `Forbid`. ADR §4.1 (G5/G18).
                    enum:
                    - Allow
                    - Replace
                    - Forbid
                    type: string
                  cron:
                    description: Cron expression with Jenkins-style `H` substitution. ADR §4.1 (G4).
                    type: string
                  jitter:
                    description: Deterministic jitter (Go-style duration), derived from `(scheduleUID, slot)`. ADR §4.1.
                    nullable: true
                    type: string
                  runOnCreate:
                    description: 'GitOps-friendly default: do NOT fire immediately on create. ADR §4.1 (G3).'
                    type: boolean
                  startingDeadlineSeconds:
                    format: int64
                    nullable: true
                    type: integer
                  suspend:
                    description: Skip future firings while true. ADR §5.9.
                    type: boolean
                  timezone:
                    nullable: true
                    type: string
                required:
                - cron
                type: object
            required:
            - configRef
            - schedule
            type: object
          status:
            nullable: true
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consecutiveFailures:
                format: int64
                nullable: true
                type: integer
              lastSchedule:
                description: Most recent firing (cron + jitter, pinned). ADR §3.5.
                nullable: true
                properties:
                  at:
                    nullable: true
                    type: string
                  backupRef:
                    nullable: true
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                type: object
              lastSuccessfulSchedule:
                description: |-
                  A pinned schedule slot and (optionally) the `Backup` it created. ADR §3.5.

                  `at`/`scheduledAt` are both accepted on the wire: ADR uses `scheduledAt` for
                  `lastSchedule` and `at` for `next`/`lastSuccessful`. We model both as the single
                  `at` field with a serde alias so either spelling round-trips.
                nullable: true
                properties:
                  at:
                    nullable: true
                    type: string
                  backupRef:
                    nullable: true
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                type: object
              nextSchedule:
                description: |-
                  A pinned schedule slot and (optionally) the `Backup` it created. ADR §3.5.

                  `at`/`scheduledAt` are both accepted on the wire: ADR uses `scheduledAt` for
                  `lastSchedule` and `at` for `next`/`lastSuccessful`. We model both as the single
                  `at` field with a serde alias so either spelling round-trips.
                nullable: true
                properties:
                  at:
                    nullable: true
                    type: string
                  backupRef:
                    nullable: true
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                type: object
              observedGeneration:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: BackupSchedule
        type: object
    served: true
    storage: true
    subresources:
      status: {}
